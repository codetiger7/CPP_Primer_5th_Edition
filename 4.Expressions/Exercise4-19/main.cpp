// Exercise 4.19
// explain behaviour
// show likely incorrect ones
// explain why/how to correct
#include <iostream>

using namespace std;

int main()
{
   // a)
   // ptr != 0 && *ptr++
   // checks if we have a nullptr
   // if we dont checks that content of ptr is not 0, then adds one to ptr
   //
   // not sure

   // b)
   // ival++ && ival : checks if ival is not 0,then increments, same on other except
   // incrementing
   // undef?

   // c)
   // vec[ival++] <= vec[ival]
   // undef



   return 0;
}

