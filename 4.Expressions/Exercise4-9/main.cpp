// Exercise 4.9
// explain behaviour

#include <iostream>

using namespace std;

int main()
{
   const char* cp  = "Hello World";

   if (cp && *cp)
      ;


   // first we check if cp is a nullptr
   // if not we check the rv, to see if the first letter in the array is zero
   // if not we execute the if body



   return 0;
}

