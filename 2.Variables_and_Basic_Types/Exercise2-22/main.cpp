// Exercise 2.22
// if p is a pointer to int
// explain code
#include <iostream>

using namespace std;

int main()
{
    if (p) // ...
        // if p is not a nullptr then execute if

        if (*p)     // if the value that p points to is not zero then execute if
    return 0;
}

